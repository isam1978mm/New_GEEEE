name: McMaster as-built pixel screen

on:
  pull_request:
    paths:
      - "scripts/extract_mcmaster_asbuilt_cover_drawings.py"
      - ".github/workflows/mcmaster-asbuilt-pixel-screen.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  extract-asbuilt:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install extraction dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pymupdf pillow

      - name: Download official NYSDEC final engineering report
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts/mcmaster_asbuilt_cover_drawings
          url='https://extapps.dec.ny.gov/data/DecDocs/706010/Report.HW.706010.2021-09-10.Final%20Engineering%20Report%20except%20Appendix%20D.pdf'
          curl --location --fail --show-error --silent \
            --retry 5 --retry-all-errors --retry-delay 5 \
            --connect-timeout 30 --max-time 1200 \
            --user-agent 'Mozilla/5.0 (compatible; environmental-evidence-recovery/1.0)' \
            "$url" --output /tmp/mcmaster_final_engineering_report.pdf \
            2>&1 | tee artifacts/mcmaster_asbuilt_cover_drawings/download.log
          file /tmp/mcmaster_final_engineering_report.pdf | tee -a artifacts/mcmaster_asbuilt_cover_drawings/download.log
          bytes=$(stat -c%s /tmp/mcmaster_final_engineering_report.pdf)
          mime=$(file -b --mime-type /tmp/mcmaster_final_engineering_report.pdf)
          if [ "$bytes" -lt 10000000 ] || [ "$mime" != 'application/pdf' ]; then
            echo "Invalid PDF: bytes=$bytes mime=$mime"
            exit 1
          fi

      - name: Extract as-built cover drawings
        shell: bash
        run: |
          set -o pipefail
          MCMASTER_APPENDICES_PDF=/tmp/mcmaster_final_engineering_report.pdf \
            python scripts/extract_mcmaster_asbuilt_cover_drawings.py \
            2>&1 | tee artifacts/mcmaster_asbuilt_cover_drawings/extractor.log

      - name: Remove source PDF
        if: always()
        run: rm -f /tmp/mcmaster_final_engineering_report.pdf

      - name: Upload evidence artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mcmaster-asbuilt-cover-drawings
          path: artifacts/mcmaster_asbuilt_cover_drawings/
          if-no-files-found: warn
          retention-days: 7
