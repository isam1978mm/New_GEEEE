name: Tyrone Dam 3X cover recovery

on:
  pull_request:
    branches: [main]
    paths:
      - "scripts/recover_tyrone_dam3x_cover_records.py"
      - ".github/workflows/tyrone-dam3x-cover-recovery.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  recover-records:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install recovery dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y poppler-utils
          python -m pip install --disable-pip-version-check requests
      - name: Recover official Tyrone records
        run: python scripts/recover_tyrone_dam3x_cover_records.py
      - name: Upload evidence package
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tyrone-dam3x-cover-records
          path: artifacts/tyrone_dam3x_cover_records/
          if-no-files-found: warn
          retention-days: 7
