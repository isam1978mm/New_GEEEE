# Auburn McMaster Radar-Linkage Execution Status — 2026-07-26

**Branch:** `main`  
**Status:** site screen inconclusive at geometry and finite-depth-label gate; move to John Sevier

## Evidence confirmed

The official construction-completion and periodic-review records confirm:

- remediation was completed from 2015 through December 2018;
- licensed construction surveying was performed;
- Appendix A contains survey maps, as-built drawings, excavation/backfill geometry and local soil-cover thickness;
- the general cover system contains at least 12 inches of clean backfill;
- a demarcation geotextile separates approved reuse material from clean cover where reuse material remains;
- a roughly 25-foot ecological buffer has two feet of clean vegetated soil;
- the later site is described as a vacant compacted-gravel lot;
- the 2024-2025 review found the cover intact, with no erosion or bare spots and no cover maintenance required.

## Access and visibility result

The files that contain the actual local thickness values and mapped as-built subareas are publicly listed but could not be rendered or downloaded through the available routes:

- the 518 MB Construction Completion Report Appendices A through G;
- the 105 MB Site Management Plan;
- the 106 MB Final Engineering Report excluding Appendix D;
- the small easement drawing also failed visual rendering.

The current periodic-review report names Figure 3.1 and Figure 3.2 as the site/remedy layout and cover-system/demarcation-layer figures. Parsed text confirms their purpose, but the figure pages could not be visually rendered. No unseen map was treated as reviewed geometry.

## Depth-label decision

The readable public text supplies:

- a general minimum of 12 inches of cover;
- a two-foot ecological-buffer requirement;
- a condition that approved reuse material remains below a demarcation layer.

These statements do not provide two comparable upland gravel subareas with actual finite measured local cover depths. The ecological buffer also differs in vegetation and setting, so it cannot serve as the deeper member of a controlled radar-depth comparison.

The `>2 ft` reuse-material condition is not a finite depth label and is not used as one.

## Auburn result

```text
actual_local_thickness_values_known_to_exist = yes
actual_local_thickness_values_readable = no
as_built_geometry_known_to_exist = yes
as_built_geometry_visually_reviewed = no
comparable_depth_ordering_zones_available = no
accepted_anchor_dates_available = no
earth_engine_query_executed = no
site_surface_response_decision = site_screen_inconclusive
site_depth_ordering_decision = site_depth_ordering_not_supported_or_inconclusive
inconclusive_reason = visible_as_built_geometry_and_finite_local_depth_labels_unavailable
cross_site_depth_linkage_decision = not_evaluated
```

Auburn remains on hold for a genuinely readable Appendix A or equivalent as-built extraction. It is not rejected as a future candidate.

## Next action

Proceed directly to John Sevier Bottom Ash Pond. Do not resume generic candidate searching.

John Sevier is the next fixed candidate because official records state that 24 inches of soil cover was placed over a large eastern geomembrane cap and later inspections support a stable period. The immediate test is whether a visually reviewable closure-limits or record-drawing map and a defensible matched comparison area can be recovered.

## Depth boundary

```text
usable_calibration_rows = 0
calibration_record_created = false
training_started = false
depth_measured = false
numerical_depth_ready = no
app_depth_enabled = false
```
