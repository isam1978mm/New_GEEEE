# F188 — DEQ NextRequest public release-library screen

Date: 2026-08-21

## Purpose

Before filing a new Virginia DEQ records request, check the public DEQ NextRequest release library for previously fulfilled requests containing the Bremo West Ash Pond / SWP618 survey records identified in F185-F187.

## Official DEQ portal status

Virginia DEQ states that beginning May 28, 2026, fulfilled requests and released records are made publicly searchable through its NextRequest portal.

The live DEQ NextRequest landing page currently reports more than 1,500 requests and provides a public `Search` route.

For Bremo/Fluvanna records, DEQ identifies the Valley Regional Office as the relevant regional department.

## Bounded search terms tested

External/indexed search was run for combinations of:

- `SWP618`
- `Bremo`
- `West Ash Pond`
- `RYAN-BREMO`
- `VC-01`
- `CBR-01`
- `CBR-02`
- `Flora Surveying Associates`

including searches scoped to `vadeq.nextrequest.com`.

## Result

No indexed public-search result exposed a previously fulfilled NextRequest request or released file matching the Bremo/SWP618 identifiers above.

However, the NextRequest request-results page itself is client-rendered/non-text-accessible in the current environment. The portal's search interface could be opened, but its individual search results could not be programmatically read here.

Therefore this step **cannot prove that no matching prior release exists inside the portal**.

## F188 decision

### Prior released record: NOT RECOVERED

### Portal completeness decision: ACCESS-LIMITED

Use the precise interpretation:

> No Bremo/SWP618 Flora survey release was recovered from externally indexed NextRequest content, but the portal's internal search results were not accessible enough in this environment to establish a definitive absence.

Do **not** document this as `no prior request exists`.

## DEQ route remains ready

If a new request is authorized, use the F187 Valley Regional Office request package and ask specifically for:

1. `SWP618 Closure by Removal Construction Rpt- West Ash Pond Part 2 of 2`;
2. any electronic record drawings/survey attachments for Flora `RYAN-BREMO`;
3. source data for `VC-01`, `CBR-01`, and `CBR-02`;
4. native point/CAD/LandXML/TIN/thickness-comparison files or a transmittal/file index identifying them.

DEQ's own guidance supports requesting existing electronic records with reasonable specificity and requesting an advance cost estimate.

## Next action — F189

Before asking the user to authorize a new outbound request, check already-connected project correspondence for an existing Bremo/Dominion/DEQ/Flora/legacy-Golder message or prior request that could already contain a contact, response, attachment, or case/request number.

This is a read-only duplicate-avoidance check. Do not send, reply, forward, or create a new request without explicit authorization.
